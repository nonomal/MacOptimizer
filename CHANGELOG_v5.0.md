# Mac优化大师 v5.0 更新说明

**发布日期：** 2026-08-02

**适用系统：** macOS 12.0 及以上

**支持架构：** Apple Silicon 与 Intel

## 版本概览

v5.0 是一次面向完整产品体验的重大更新。本版本重新梳理主窗口、悬浮侧边栏、详情页和底部操作区，补齐真实扫描结果与文件操作，并加入全局 Mac 优化智能体和完整的六语言界面。

## 全新 Mac 优化智能体

- 新增全局 Mac 优化智能体，可从任意功能页面打开并理解当前模块。
- 智能体可自主选择本机工具，检查垃圾文件、磁盘空间、系统状态、启动项、大型文件与重复文件。
- 重复文件扫描采用“文件大小预筛选 + SHA-256 完整内容校验”，只把完全相同的冗余副本计入结果。
- 支持 OpenAI、Gemini、Claude 三种接口协议，也支持自定义 Base URL、模型名称和兼容中转服务。
- 支持粘贴、拖放和选择图片，并提供缩略图预览与移除操作。
- API 配置保存在本机普通 JSON 文件中，不使用钥匙串；界面不会显示或泄露完整密钥。
- 本机文件路径不会发送给第三方模型，只发送必要的数量、分类与大小摘要。
- 所有清理操作必须由用户确认；允许清理的文件优先移入废纸篓，不执行不可恢复的永久删除。
- 更新聊天与设置界面，最新回复后可直接清除聊天记录。
- 模型设置页新增“造梦影视与设计平台”入口，可访问 [zaomeng.art](https://zaomeng.art) 体验 AI 生图、AI 生视频、AI 设计智能体和 AI 导演智能体。

## 智能扫描与清理能力

- 智能扫描重新整合系统垃圾、邮件附件、废纸篓、恶意软件、大型文件、磁盘空间和重复文件结果。
- 扫描过程中实时更新当前状态；扫描结束后在侧边栏同步显示扫描结果大小。
- 系统垃圾扫描扩展用户缓存、日志、崩溃报告、系统缓存、文稿版本、Xcode 数据和其他可安全识别的垃圾类别。
- 深度清理、隐私、邮件附件、废纸篓和恶意软件模块补充真实选择、统计与清理反馈。
- 大型和旧文件支持读取真实磁盘与用户选择的扫描目录，扫描结果可继续进入对应目录。
- 空间透镜读取当前 Mac 的真实卷和目录结构，并支持逐级浏览扫描结果。
- 扫描结果统一提供文件级右键菜单，可在访达中显示、打开、查看信息或复制路径。

## 界面与交互

- 主窗口采用固定悬浮侧边栏与内容区域叠层布局，移除旧版多余菜单结构。
- 统一各模块底部主按钮的尺寸、位置、悬停、按下、运行中和完成状态。
- 详情页左侧分类与右侧文件列表建立同步选择关系；点击父级分类可正确选择对应结果。
- 修复侧边栏切换延迟、多项同时高亮、内容重复加载和界面闪烁问题。
- 模块切换时只刷新内容区域，侧边栏保持固定，减少无效重绘和卡顿。
- 统一详情弹框、三栏结果页、文件统计、选择按钮和底部操作区的视觉层级。
- 优化碎纸机、大型和旧文件、空间透镜、邮件附件、系统垃圾、控制台等模块的布局与交互。
- 更新欢迎页和各功能模块插图，使透视方向、比例和背景色更协调。

## 控制台与菜单栏

- 优化控制台总览、侧边栏和各管理详情页。
- 完善 CPU、内存、磁盘、网络、电池和存储详情展示。
- 改进 macOS 菜单栏下拉监控、内存提醒与监控设置。
- 控制台只展示真实可读取状态，不生成虚构的广告拦截或系统统计数据。

## 多语言

- 完整支持简体中文、繁体中文、英语、日语、韩语和俄语。
- 首次启动提供语言选择，后续可从 macOS 应用菜单切换。
- 主窗口、模块页、详情页、按钮、右键菜单、弹框、错误信息、扫描状态和智能体输出均跟随当前语言。
- 新增严格本地化审计，阻止两语言残留和未完成的动态文案进入发布版本。

## 安全与隐私

- 清理前保留明确的用户确认步骤。
- 支持的文件清理优先移动到系统废纸篓，便于恢复。
- 大型文件、重复文件、启动项等高风险结果不会由智能体自动删除。
- API Key 与 Base URL 仅保存在当前 Mac，不写入项目源码，也不会随版本提交。
- 优化扫描路径和系统保护规则，降低误删系统文件及用户重要数据的风险。

## 兼容性与发布

- 应用版本统一更新为 5.0。
- 最低支持 macOS 12.0。
- 发布脚本与 Homebrew 配方版本号同步为 5.0。
- 支持 Apple Silicon 与 Intel 双架构发布流程。

## 升级提示

- 从旧版本升级后，建议首次打开时重新确认语言和 AI 模型连接配置。
- AI 模型连接由用户自行提供；未配置模型时，本机已有清理模块仍可独立使用。
- 执行清理前请检查选中项目。对于大型文件和重复文件，建议先在访达中确认内容。
